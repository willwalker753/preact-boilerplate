{
  "private": true,
  "name": "preact-boilerplate",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "dev": "preact watch"
  },
  "devDependencies": {
    "preact-cli": "^3.0.0"
  },
  "dependencies": {
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1"
  }
}
